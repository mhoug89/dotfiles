# Add `go` binary's directory to our PATH var.
GO_BIN_PARENT_DIR="$(go env GOPATH 2>/dev/null)"
if [ -d "${GO_BIN_PARENT_DIR}/bin" ]; then
  export PATH="${PATH}:${GO_BIN_PARENT_DIR}/bin"
fi
unset GO_BIN_PARENT_DIR
